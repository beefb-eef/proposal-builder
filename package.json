{
  "name": "proposal-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Proposal builder (Node + Render) that generates HTML previews and downloadable PDFs.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development nodemon server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "handlebars": "^4.7.8",
    "puppeteer": "^23.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
