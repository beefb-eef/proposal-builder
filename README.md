# Proposal Builder (Node + Render)

A web app that:
- Collects quote inputs (Maps, AI Chat, or both)
- Renders a proposal HTML preview
- Exports a pixel-matching PDF via Puppeteer

## Local dev
```bash
npm install
npm run dev
